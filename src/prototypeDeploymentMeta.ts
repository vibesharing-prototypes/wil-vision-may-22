/** Static identity for this build — see PROTOTYPES.md at repo root. */
export const PROTOTYPE_DEPLOYMENT_META = {
  channelLabel: "Vision prototype",
  purposeLine:
    "Schema management, roles, and workflows — for design and engineering iteration (BOS and spikes live in Lab).",
} as const;
